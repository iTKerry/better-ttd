<UserControl x:Class="BetterTTD.WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding LoginViewModel, Source={StaticResource Locator}}">
    <Grid>
        <materialDesign:Card Width="300" Height="200" Padding="32" Margin="16">
            <StackPanel>
                <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Margin="0,0,0,20">My First Material Design App</TextBlock>
                <Button Command="{Binding LoginCommand}">Login</Button>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>

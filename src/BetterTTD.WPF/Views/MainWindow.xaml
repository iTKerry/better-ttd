<Window x:Class="BetterTTD.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:BetterTTD.WPF.ViewModels"
        xmlns:views="clr-namespace:BetterTTD.WPF.Views"
        mc:Ignorable="d"
        Title="BetterTTD" Height="450" Width="800"
        DataContext="{Binding MainWindowViewModel, Source={StaticResource Locator}}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels:ConnectViewModel}">
            <views:ConnectView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
    </Window.Resources>
    <Window.Content>
        <Grid>
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Grid>
    </Window.Content>
</Window>
